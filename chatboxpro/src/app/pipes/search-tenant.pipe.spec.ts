import { SearchTenantPipe } from './search-tenant.pipe';

describe('SearchTenantPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTenantPipe();
    expect(pipe).toBeTruthy();
  });
});
